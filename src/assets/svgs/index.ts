import SearchIcon from './search.svg';
import SortDownIcon from './sortDown.svg';
import CrossIcon from './cross.svg';

// mock images that should be removed, because they will be fetched from the api

export {SearchIcon, SortDownIcon, CrossIcon};
